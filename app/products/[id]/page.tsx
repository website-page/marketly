import Link from 'next/link'; import {notFound} from 'next/navigation'; import {products} from '@/lib/data';
export default async function Product({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=products.find(x=>x.id===id);if(!p)return notFound();return <main className="page"><div className="wrap" style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:40,alignItems:'center'}}><div className="productImg" style={{height:480,borderRadius:24,backgroundImage:`url(${p.image})`}}/><div><span className="pill">{p.category}</span><h1 className="pageTitle" style={{fontSize:52}}>{p.title}</h1><p className="pageSub">{p.description}</p><div style={{fontSize:36,fontWeight:800,margin:'28px 0'}}>${p.price}</div><Link href={`/checkout?product=${p.id}`} className="button">Buy securely</Link><div className="notice" style={{marginTop:20}}>Digital delivery is granted only after successful server-side payment verification.</div></div></div></main>}