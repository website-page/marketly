import Link from 'next/link'; import {services} from '@/lib/data';
export default function Services(){return <main className="page"><div className="wrap"><h1 className="pageTitle">Services</h1><p className="pageSub">Need something custom? Work with a Marketly creator to turn an idea into a polished digital result.</p><div className="grid" style={{marginTop:40}}>{services.map(([title,desc,price])=><div className="card" key={title}><div className="cardBody"><span className="pill">CREATIVE SERVICE</span><h3>{title}</h3><p>{desc}</p><div className="row"><b>{price}</b><Link href="/signup" className="button small">Start enquiry</Link></div></div></div>)}</div></div></main>}